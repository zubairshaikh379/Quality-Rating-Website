specVersion: "v1"
serviceId: "quality-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quality-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
